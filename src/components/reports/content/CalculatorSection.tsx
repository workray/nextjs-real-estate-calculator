'use client'

const CalculatorSection = ({ children }: { children: React.ReactNode }) => {
  return <section className="flex flex-col space-y-4 m-4">{children}</section>
}
export default CalculatorSection
